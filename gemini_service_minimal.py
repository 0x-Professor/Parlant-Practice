"""
Minimal Gemini NLP Service for Parlant - Just to test basic functionality
"""

print("Creating minimal gemini service...")

import os
from dotenv import load_dotenv

# Load environment variables
load_dotenv()

def load_gemini_nlp_service(container):
    """Minimal factory function - will be replaced with full implementation once working."""
    print("Creating minimal Gemini NLP service...")
    
    # For now, return None - this is just to test the import
    # We'll expand this once we confirm the import structure works
    return None

print("Minimal gemini service created successfully!")
